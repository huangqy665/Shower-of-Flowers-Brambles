root_ruleset = {
    name = dillen.hoi3.1936_world_root
    version = 1
    required_packages = {
        requirement = { name = dillen.map.world.contracts  minimum_major = 1 minimum_minor = 0 minimum_patch = 0  maximum_major = 2 maximum_minor = 0 maximum_patch = 0 }
        requirement = { name = dillen.map.world  minimum_major = 1 minimum_minor = 0 minimum_patch = 0  maximum_major = 2 maximum_minor = 0 maximum_patch = 0 }
        requirement = { name = dillen.country.contracts  minimum_major = 1 minimum_minor = 0 minimum_patch = 0  maximum_major = 2 maximum_minor = 0 maximum_patch = 0 }
        requirement = { name = dillen.hoi3.1936_world  minimum_major = 1 minimum_minor = 0 minimum_patch = 0  maximum_major = 2 maximum_minor = 0 maximum_patch = 0 }
    }
    required_components = {
        dillen.map.geography = 1
        dillen.country.identity = 1
    }
    required_relations = {
        dillen.map.borders = 1
        dillen.country.owns_region = 1
        dillen.country.controls_region = 1
        dillen.country.core_on_region = 1
        dillen.country.capital_region = 1
    }
    required_entity_definitions = { all = yes }
    required_relation_definitions = { all = yes }
}
