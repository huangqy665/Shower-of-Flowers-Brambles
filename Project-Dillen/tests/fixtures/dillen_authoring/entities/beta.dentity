entity_definition = {
    name = dillen.demo.beta
    entity_type = dillen.demo.actor
    components = {
        component = {
            type = dillen.demo.identity
            schema_version = 1
            fields = { value = 2 }
        }
    }
}
