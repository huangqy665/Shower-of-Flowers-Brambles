mechanism_definition = {
    name = dillen.g.alvara_budget
    mechanism = dillen.g.national_budget
    schema_version = 1
    algorithm = dillen.g.budget_algorithm
    algorithm_version = 1
    fields = { upkeep = 4.00 }
    roles = {
        capital = {
            entity = { entity_type = dillen.g.country  definition = dillen.g.alvara }
        }
    }
}
