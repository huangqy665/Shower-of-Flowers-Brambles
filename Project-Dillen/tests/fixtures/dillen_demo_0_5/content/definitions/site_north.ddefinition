mechanism_definition = {
    name = dillen.g.site_north
    mechanism = dillen.g.production_site
    schema_version = 1
    algorithm = dillen.g.production_algorithm
    algorithm_version = 1
    roles = {
        province = {
            entity = { entity_type = dillen.g.province  definition = dillen.g.north_reach }
        }
    }
}
