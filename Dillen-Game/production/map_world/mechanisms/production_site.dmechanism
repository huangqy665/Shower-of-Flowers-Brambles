mechanism_template = {
    name = dillen.map.production_site
    version = 1
    fields = {
        field = { name = level   kind = integer  required = yes  default = 1 }
        field = { name = output  kind = decimal  required = yes  default = 0.0 }
    }
    roles = {
        role = { name = province  reference_kind = entity  minimum_count = 1  maximum_count = 1 }
    }
}
