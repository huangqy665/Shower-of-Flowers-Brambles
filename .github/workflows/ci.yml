name: CI

on:
  push:
    branches: [ main ]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  standalone-windows-x64:
    name: Pure Dillen standalone (Windows, MSVC x64)
    runs-on: windows-2022
    defaults:
      run:
        working-directory: Project-Dillen
    steps:
      - name: Checkout (engine tree only)
        uses: actions/checkout@v4
        with:
          # Skip the large reference repo and mod-content directories.
          sparse-checkout: Project-Dillen

      - name: Configure
        run: cmake --preset dillen-standalone-windows-x64

      - name: Build
        run: cmake --build --preset dillen-standalone-windows-x64-debug

      - name: Test
        run: ctest --preset dillen-standalone-windows-x64-debug --output-on-failure

  standalone-linux:
    name: Pure Dillen standalone (Linux, ${{ matrix.cc }})
    runs-on: ubuntu-24.04
    # Blocking gate as of 2026-08-30. Verified locally under WSL Ubuntu 24.04
    # with the same toolchain versions this image ships (GCC 13.3, Clang 18.1,
    # CMake 3.28, Ninja 1.11): both compilers build clean and all 19 probes
    # pass. Keeping this blocking is what stops the Package `content_digest`
    # from silently diverging between platforms again -- see .gitattributes.
    strategy:
      fail-fast: false
      matrix:
        include:
          - cc: gcc
            cxx: g++
          - cc: clang
            cxx: clang++
    defaults:
      run:
        working-directory: Project-Dillen
    steps:
      - name: Checkout (engine tree only)
        uses: actions/checkout@v4
        with:
          sparse-checkout: Project-Dillen

      - name: Install Ninja
        run: sudo apt-get update && sudo apt-get install -y ninja-build

      - name: Configure
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: >
          cmake -S . -B build-linux -G Ninja
          -D CMAKE_BUILD_TYPE=Debug
          -D BUILD_TESTING=ON
          -D DILLEN_BUILD_STANDALONE_RUNTIME=ON
          -D DILLEN_BUILD_HOI3_COMPATIBILITY=OFF
          -D DILLEN_BUILD_HOI3_ORACLE=OFF

      - name: Build
        run: cmake --build build-linux

      - name: Test
        run: ctest --test-dir build-linux --output-on-failure
